name: dart_grid
description: Dart Grid type data implementation. This package allows you to work with data tables like Excel, while saving only real data (empty data is only generated).
version: 1.0.1
repository: https://gitlab.com/dipdev.studio/open-source/dart-grid

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  collection: ^1.17.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
